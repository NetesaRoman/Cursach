

<li><%= link_to 'Create genre', new_genre_path%></li>
<li><%= link_to 'Back to movies', movies_path%></li>


  <div >
    <table >
      <thead >
      <tr>
        <th >Genre Name</th>
        <th>Action</th>
      </tr>
      </thead>
      <tbody>
      <% @genres.each do |genre| %>
        <tr>
          <td><%= genre.name %></td>
          <td><%= button_to 'Delete', genre_path(genre.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>


      <% end %>
      </table>
  </div>

