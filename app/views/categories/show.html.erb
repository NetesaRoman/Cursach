<div>

  <div>

    <h2>Name:</h2>
    <p><%= @category.name %></p>
    <h2>Url:</h2>
    <p><%= @category.url %></p>
    <h2>Id:</h2>
    <p><%= @category.id %></p>
    <h2>Description:</h2>
    <p><%= @category.description %></p>



  </div>
  <%= link_to "Back to list", root_path %>

  <div id="root_buttons" >
    <td><%= button_to 'Edit', edit_category_path(@category), method: :get, id: 'rootBtn' %></td>
    <td><%= button_to 'Remove', category_path(@category), method: :delete, data: { confirm: 'Are you sure?' }, id: 'rootBtn' %></td>
  </div>
</div>
