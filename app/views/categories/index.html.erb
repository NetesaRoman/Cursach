

<li><%= link_to 'Create Category', new_category_path%></li>
<li><%= link_to 'Back to movies', movies_path%></li>


  <div >
    <table >
      <thead >
      <tr>
        <th >Category Name</th>
        <th>Action</th>
      </tr>
      </thead>
      <tbody>
      <% @categories.each do |category| %>
        <tr>
          <td><%= category.name %></td>
          <td><%= button_to 'Delete', category_path(category.id), method: :delete, data: { confirm: 'Are you sure?' }  %></td>


      <% end %>
      </table>
  </div>

